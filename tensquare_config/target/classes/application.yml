spring:
  application:
    name: tensquare-config
  clound:
    config:
      server:
        git:
          uri: https://github.com/StarRoving/tensquare.git
server:
  port: 12000
rabbitmq:
  host: 127.0.0.1
management: #暴露触发消息总线的地址
  endpoints:
    web:
      exposure:
        include: bus-refresh